---
import SiteFooter from '../components/site/SiteFooter.astro';
import SiteHeader from '../components/site/SiteHeader.astro';
import TabIdentity from '../components/site/TabIdentity.astro';
import BackToTop from '../components/site/BackToTop';
import PageScrollIndicator from '../components/site/PageScrollIndicator';
import ScrollManager from '../components/site/ScrollManager';
import StarfieldBackground from '../components/starfield/StarfieldBackground';
import { SITE_CONFIG } from '../config/site';
import '../styles/global.scss';

type Props = {
	title?: string;
	description?: string;
	starfield?: boolean;
	starfieldVariant?: 'immersive' | 'subtle';
};

const {
	title = SITE_CONFIG.defaultTitle,
	description = SITE_CONFIG.defaultDescription,
	starfield = true,
	starfieldVariant = 'immersive',
} = Astro.props;
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="preconnect" href="https://assets.telysta.com" crossorigin />
		<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
		<link rel="icon" type="image/png" sizes="192x192" href="/favicon-192x192.png" />
		<link rel="icon" href="/favicon.ico" />
		<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
		<meta name="description" content={description} />
		<title>{title}</title>
		<TabIdentity />
	</head>
	<body>
		{starfield && <StarfieldBackground client:idle variant={starfieldVariant} />}
		<SiteHeader />
		<div class="site-shell">
			<slot />
		</div>
		<SiteFooter />
		<PageScrollIndicator client:idle />
		<BackToTop client:idle />
		<ScrollManager client:idle />
	</body>
</html>
