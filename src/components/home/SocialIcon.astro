---
import type { HomeSocialIcon } from '../../config/pages/home';

type Props = {
	name: HomeSocialIcon;
};

const { name } = Astro.props;

import { SOCIAL_ICON_PATHS } from '../../config/visuals/socialIcons';

---

<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
	<path d={SOCIAL_ICON_PATHS[name]}></path>
</svg>
